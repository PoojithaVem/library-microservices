# notification-service currently has nothing beyond the shared application.yml
# above (which already provides app.kafka.topics.loan-events). Kept as a
# placeholder for future centralized settings, e.g. notification retry policy.
